*.iml
.gradle/
.kotlin/
/local.properties
/.idea/
.DS_Store
/captures
.externalNativeBuild
.cxx
local.properties
*.hprof

# Every module's build output (app, core/*, feature/*), not just app's --
# matches at any depth so new modules don't need a manual entry here.
**/build/
release/

# Local Claude Code state (permissions, scheduled-task lock) -- machine-local,
# not project source.
.claude/


app-release-signed.apk.idsig
README-CONTEXT.md

# Signing material -- never commit the keystore or its credentials.
keystore/
keystore.properties